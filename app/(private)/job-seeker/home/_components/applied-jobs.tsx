import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const AppliedJobs = () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
      }}
    >
      <Text>AppliedJobs</Text>
    </SafeAreaView>
  );
};

export default AppliedJobs;
